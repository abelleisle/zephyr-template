description: Soil Moisture Sensor

# Soil Moisture is ADC
compatible: "soil,soil_moisture"

include: [sensor-device.yaml, adc-controller.yaml]

properties:
  io-channels:
    required: true
    description: ADC channel for soil moisture sensor
